type refdomain = {
  refdomain: string;
  backlinks: int;
  refpages: int;
  first_seen: string;
  last_visited: string;
  domain_rating: int;
}

type stats = {
  refdomains: int;
  ips: int;
  class_c: int;
}

type response = {
  refdomains: refdomain list;
  stats: stats;
}
